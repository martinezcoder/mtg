#!/usr/bin/env ruby

lib_dir = File.expand_path("#{__dir__}/../lib/")
$LOAD_PATH.unshift(lib_dir) unless $LOAD_PATH.include?(lib_dir)

require 'lingo_kids'
require 'pry'
#res =  LingoKids.run #(ARGV[0])


binding.pry
puts "hola"
